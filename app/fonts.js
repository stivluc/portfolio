import { Inter, Roboto_Mono } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

const hemingSans = localFont({
  src: './fonts/Heming.ttf',
  display: 'swap',
});
